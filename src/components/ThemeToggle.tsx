import { motion } from "framer-motion";

const ThemeToggle = () => {
  return (
    <header my-6 flex="~ row justify-end">
      <motion.div className="bg-white w-4 h-5" />
    </header>
  );
};

export default ThemeToggle;
